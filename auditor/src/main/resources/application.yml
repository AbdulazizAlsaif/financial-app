server:
  servlet:
    context-path: /audit-svc/api

spring:
  output:
    ansi:
      enabled: ALWAYS
  data:
    mongodb:
      auto-index-creation: true
      uri: mongodb://host.docker.internal:27018/
      database: financial-audit
